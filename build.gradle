version 1.0

subprojects {
    apply plugin: 'java'
    repositories {
        mavenCentral()
    }
    dependencies {
        if (name != 'common') {
            compile project (':common')
        }
    }
}